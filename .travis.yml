language: go

go:
  - 1.15.x

before_script:
  - go get -u go get -u golang.org/x/lint
  - go get -u golang.org/x/tools/go/analysis/passes/shadow/cmd/shadow

script:
  - ./code-check.sh
  - ./coverage.sh --integration

after_success:
  - bash <(curl -s https://codecov.io/bash)
